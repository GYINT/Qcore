apiVersion: v1
kind: Service
metadata:
  name: {{ include "qcm-mcp.fullname" . }}
  labels:
    {{- include "qcm-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qcm-mcp.selectorLabels" . | nindent 4 }}
